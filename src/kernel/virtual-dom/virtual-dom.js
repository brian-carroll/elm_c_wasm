var VirtualDom_applyPatches = function (patchesStartAddr) {
  console.log(`Proper JS VirtualDom_applyPatches: 0x${patchesStartAddr.toString(16)}`);
  console.log('typeof document:', typeof document);
  console.log('#main:', document.getElementById('main'));
}
